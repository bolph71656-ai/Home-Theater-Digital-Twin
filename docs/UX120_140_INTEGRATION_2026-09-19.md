# UX120–UX140 workspace integration — 2026-09-19

Issue #118 / Issue #102 の並列sliceを main へ取り込み、新しいworkflow shellで実際に
利用できる状態へ接続したintegration記録。

## Integrated PRs

- #128 — Settings / Data Management UI
- #129 — reusable CAD input / shortcut controller
- #130 — viewport-centric Room workspace
- #132 — page-first Measurements workspace
- #131 — Optimization page compositionはparallel branchが#129と競合したため、
  component内容を本integration branchへ取り込み、canonical navigation変更をここで解決

## Workflow composition

`workflow_application.py` をapplication composition rootとして追加した。

`--workflow-shell` 起動時は以下をlazy mountする。

- 概要: readiness-driven `OverviewWorkspace`
- 部屋: `RoomWorkspace`
- 測定: `MeasurementPageWorkspace`
- 最適化: `OptimizationWorkflowWorkspace`

shell/routerはdomain semanticsを持たず、workspace factory / command binding / data lifecycleだけを
composition rootが接続する。

accepted legacy `OptimizationWorkspaceWindow` は通常起動のdefaultとして維持する。
UX150/UX160のWindows visual acceptance前にlauncher authorityを切り替えない。

## UX120 Room

新Room workspaceへ以下を接続した。

### Camera / pointer

- MMB: pan
- Shift + MMB: orbit
- wheel: zoom
- RMB: central command registry由来のcontext menu

### Commands

- M: 選択objectの移動をarm
- R: 選択objectの回転をarm
- X / Y / Z: active transformのaxis constraint
- F: 選択へfocus
- Home: scene fit
- Esc: transform / room geometry operation cancel
- Enter: active operation commit
- Ctrl+D: duplicate
- Ctrl+S / Ctrl+Z / Ctrl+Y: active Room WorkingDocumentへbind

M/R dragは `TheaterWorkingDocument.begin_* / preview_* / commit_preview()` を使用する。
viewportは `working.document` を描画するためpreviewが即時表示される。
commit後だけrecovery snapshotを更新し、Undoは既存CommandHistoryを使用する。

### Room geometry

`RoomGeometryInputController` を追加し、new Room viewport上で:

- polygon vertex clickによるroom作図
- first vertex / Enterでclose
- Top viewで既存vertex drag
- invalid polygon reject
- Escで終了/cancel

を実装した。transient pointer stateだけをcontrollerが所有し、valid snapshotのcommitは
`RoomWorkingDocument.replace_room()` に委譲する。

### N70 prediction integration

follow-upで既存N70 authorityを新Roomの「音響」contextへ接続した。

- existing request identity / JobGuard / CadPredictionRepositoryを再利用
- current SceneRevision / content hash / constraint workspace hashへexact binding
- background worker + cancel
- stale/delayed result reject
- saved runの現在/要再計算表示
- current runだけdirect/first-reflection geometry overlay表示
- central `prediction.run` commandを新Roomへbind

詳細: [UX120 Room prediction integration](UX120_ROOM_PREDICTION_2026-09-19.md)

### Remaining Room limitations

以下は未完了を隠さない。

- 新geometry surfaceには旧Room/Wall editorが持つmidpoint insert/delete、live edge dimension、
  wall/opening topologyの全機能をまだ移植していない。
- UX150/UX160のlighting / DPI / hit-target / visual acceptanceは未実施。

## UX130 Measurements

legacy measurement dockはworkflow shellから外し、4 page workspaceへ置換した。

1. 読み込み
2. 割り当て
3. 品質
4. 比較

REW parser/import、CadMeasurementRepository、quality/capability、comparison authorityは既存実装を再利用。
REW background job実行中はworkspace deactivation / destructive restoreをfail closedする。

## UX140 Optimization

canonical contextsを4 pageへ更新した。

- `setup` — 探索設定
- `candidates` — 候補
- `comparison` — 比較
- `validation` — 測定・検証

旧deep-linkはshell boundaryで:

- `objectives -> comparison`
- `measurement-plan -> validation`

へnormalizeする。

O10〜O80のrepository/service/controller authorityは変更しない。
`optimization.compare_candidates` はCompare pageへdeep-linkする。

現在のUX140は移行adapterであり、`OptimizationWorkspaceWindow` の既存controller-created controlsを
pageへrecomposeしている。旧dock/toolbarはUX140 surfaceでは非表示だが、controller state adapterとして
内部に残る。最終的なlegacy QMainWindow継承除去は後続cleanupとする。

## Settings / Data Management

rail下部に「設定」utilityを追加し、top-level workspaceには増やしていない。

Settings dialogは#128の `DataManagementWidget` をhostし、#124の
`DataManagementController / ApplicationDataLifecycle` を利用する。

restore lifecycle:

1. shell mutation/navigation freeze
2. **一度mount済みの全workspace**の `before_deactivate` を検査
3. dirty draft / active preview / REW job等があればfail closed
4. 全workspace mountをclose/dispose
5. existing `native_backup.restore_backup()` authorityでrestore
6. fresh `SceneRepository` をconstruct
7. Overviewからlazy workspaceを再構築
8. shellをthaw

stale repository / WorkingDocumentをrestore後に再利用しない。

## Navigation authority

`workflow_navigation.py` が引き続き唯一のworkspace/context/deep-link authority。

optimization legacy aliasもここでnormalizeし、workspace固有routerを第二authorityとして作らない。

## Validation scope

Focused testsは以下のintegration invariantだけを追加する。

- optimization canonical context / legacy alias
- Settings utilityがglobal workspaceを増やさない
- restore前にhidden mounted workspaceもguardする
- Room polygon sketchがWorkingDocument/recovery経由でcommitされる
- Room M/R previewが表示snapshotへ出て、commitが1 Undo単位になる
- transform cancelがcommitted Sceneを変更しない

full repository CI / Windows Release Artifactを最終回帰authorityとする。

## Next work

1. Room geometryのmidpoint / numeric dimension / wall/opening topologyを新componentへ移植
2. UX140のlegacy QMainWindow/controller-created widget adapterを段階的に直接page componentへ置換
3. UX150 visual/language/motion/layout polish
4. UX160 owned-Windows first-use / DPI / visual acceptance
5. acceptance後にworkflow shellをdefault launcherへ昇格

RDCは本integrationでは使用しない。
